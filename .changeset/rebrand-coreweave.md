---
'@coreweave/backstage-plugin-score-card': patch
---

Rebrand for the CoreWeave fork: the Score board's `Maintainer` header label now reads `CoreWeave`, and the sample `jsonDataUrl` snippets in the README point at this fork's `sample-data/` directory instead of the upstream Oriflame repo.
